version: 0.2
phases:
  build:
    commands:
      - docker login -u $dockerhub_username -p $dockerhub_password
      - docker run --rm -v ./ -w ./ markhobson/maven-chrome:latest mvn clean verify -e

artifacts:
  files:
    - ./**/*
